diff -urN -x CYGWIN-PATCHES -x 'aclocal.m4*' -x autom4te.cache -x config.cache -x config.log -x config.status -x config.h -x config.h.in -x ABOUT-NLS -x Makefile.in.in -x Makevars.template -x '*SlackBuild*' -x '*.class' -x '*.pyc' -x '*.mo' -x '*.gmo' -x '*.orig' -x '*.rej' -x '*.spec' -x '*.temp' -x '*~' -x '*.stackdump' -x COPYING -x INSTALL -x compile -x config-ml.in -x config.guess -x config.sub -x depcomp -x elisp-comp -x install-sh -x ltmain.sh -x mdate-sh -x missing -x mkinstalldirs -x py-compile -x symlink-tree -x texinfo.tex -x ylwrap -x omf.make -x xmldocs.make -x gnome-doc-utils.make -x gnome-doc-utils.m4 -x intltool.m4 -x intltool-extract -x intltool-extract.in -x intltool-merge -x intltool-merge.in -x intltool-update -x intltool-update.in origsrc/pyvorbis-1.4/setup.py src/pyvorbis-1.4/setup.py
--- origsrc/pyvorbis-1.4/setup.py	2003-12-19 01:55:56.000000000 -0600
+++ src/pyvorbis-1.4/setup.py	2007-05-17 14:45:19.619813300 -0500
@@ -47,6 +47,7 @@
 
 ogg_include_dir = data['ogg_include_dir']
 ogg_lib_dir = data['ogg_lib_dir']
+ogg_libs = string.split(data['ogg_libs'])
 
 vorbismodule = Extension(name='vorbis',
                          sources=['src/vorbismodule.c',
@@ -61,7 +62,7 @@
                                        ogg_include_dir],
                          library_dirs=[vorbis_lib_dir,
                                        ogg_lib_dir],
-                         libraries=vorbis_libs)
+                         libraries=vorbis_libs + ogg_libs)
 
 setup ( name = "pyvorbis",
         version = pyvorbis_version,
